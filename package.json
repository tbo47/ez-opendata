{
  "name": "ez-opendata",
  "version": "1.0.1",
  "description": "Wrap open-data sources (wikipedia, openstreetmap, wikimedia, etc...) calls in simple javascript functions.",
  "main": "index.js",
  "scripts": {
    "dev": "tsc --build tsconfig.json --declaration --watch",
    "clean": "rm -rf index.js index.d.ts package-lock.json node_modules ; ncu -u ; npm i",
    "prepublishOnly": "tsc --build tsconfig.json --declaration && npm test",
    "test": "tsc --build tsconfig.test.json && node test.mjs"
  },
  "keywords": [
    "wikipedia",
    "opensteetmap",
    "wikimedia"
  ],
  "files": [
    "index.d.ts"
  ],
  "author": "tbo47",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.2.2"
  }
}